{% extends 'base.html.twig' %}

{% block title %}Modifier l'article - {{ article.titre }}{% endblock %}

{% block body %}
    <h1>Modifier l'article</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}

        <button class="btn btn-primary mt-3">Enregistrer les modifications</button>
    {{ form_end(form) }}

    <a href="{{ path('article_show', {'id': article.id}) }}" class="btn btn-link mt-3">← Retour à l'article</a>
{% endblock %}
